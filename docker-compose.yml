version: '3.8'

services:
    # Aplicação Laravel
    app:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                USER_ID: ${USER_ID:-1000}
                GROUP_ID: ${GROUP_ID:-1000}
        container_name: simhub-app
        restart: unless-stopped
        working_dir: /var/www/html
        volumes:
            - ./:/var/www/html
            - ./storage:/var/www/html/storage
            - ./bootstrap/cache:/var/www/html/bootstrap/cache
        networks:
            - simhub-network
        environment:
            - DB_CONNECTION=${DB_CONNECTION}
            - DB_HOST=${DB_HOST}
            - DB_PORT=${DB_PORT}
            - DB_DATABASE=${DB_DATABASE}
            - DB_USERNAME=${DB_USERNAME}
            - DB_PASSWORD=${DB_PASSWORD}

    # Nginx
    nginx:
        image: nginx:alpine
        container_name: simhub-nginx
        restart: unless-stopped
        ports:
            - "${SERVER_PORT:-8500}:80"
        volumes:
            - ./:/var/www/html
            - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
        networks:
            - simhub-network
        depends_on:
            - app

    # MySQL (opcional, caso queira usar MySQL ao invés do SQL Server externo)
    # mysql:
    #   image: mysql:8.0
    #   container_name: simhub-mysql
    #   restart: unless-stopped
    #   environment:
    #     MYSQL_DATABASE: ${DB_DATABASE:-simhub}
    #     MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-root}
    #     MYSQL_PASSWORD: ${DB_PASSWORD:-root}
    #     MYSQL_USER: ${DB_USERNAME:-laravel}
    #   ports:
    #     - "3306:3306"
    #   volumes:
    #     - mysql-data:/var/lib/mysql
    #   networks:
    #     - simhub-network

    # Redis (opcional, para cache e filas)
    # redis:
    #   image: redis:alpine
    #   container_name: simhub-redis
    #   restart: unless-stopped
    #   ports:
    #     - "6379:6379"
    #   networks:
    #     - simhub-network

networks:
    simhub-network:
        driver: bridge

# volumes:
#   mysql-data:
#     driver: local
